# Critique — 007-wall-labels-museum — Round 2

## Round 1 items addressed
1. Corner widget trim to 2 lines (roman + title only) — ✓ done. `.cws` now contains only `.roman` ("Object i") + `.nm` ("Coffee Shop"). The third `.asc` line is gone from the desktop widget. Reads as a small label, not as an info card.
2. Picture-light wash less LED, more incandescent — ✓ done. The active label's left-bar `box-shadow` dropped from `0 0 10px rgba(.4)` to `0 0 6px rgba(.25)`. The picture-light radial-gradient also moderated to `.16` alpha. The cone now reads as warm-bone, not greenish.
3. Rewrite start-route ascriptions in literary voice — ✓ done from the screenshot evidence. Saved-sites entries continue in literary register ("WooCommerce storefront, twelve products, twelve days in residence"). The start-routes carry the same ascription voice.

## New issues introduced
None. The Roman-numeral marker plus single bone-rule active state is now decisively a museum wall.

## Still unaddressed
Nothing structural.

## Final verdict
SHIP
